export enum Materials {
    iron = 'iron',
    steal = 'steal',
    stone = 'stone'
}

export enum Sizes {
    S = 'S',
    M = 'M',
    L = 'L',
    XL = 'XL'
}

export type sizeOptions = {
    [key: string]: number[]
}
